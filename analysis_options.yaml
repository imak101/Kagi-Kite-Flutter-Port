include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # ignore this rule according to https://pub.dev/packages/freezed#install
    invalid_annotation_target: ignore
    # false alarms for GetIt calls
    implicit_call_tearoffs: ignore